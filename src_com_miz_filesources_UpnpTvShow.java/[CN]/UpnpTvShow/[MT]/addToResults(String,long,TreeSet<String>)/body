{
  if (MizLib.checkFileTypes(file)) {
    if (size < getFileSizeLimit())     return;
    if (!clearLibrary())     if (existingEpisodes.get(file.split("<MiZ>")[1]) != null)     return;
    results.add(file);
  }
}
