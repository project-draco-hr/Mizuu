{
  try {
    FileSource fs=getFileSource();
    SmbFile root=new SmbFile(MizLib.createSmbLoginString(fs.getDomain(),fs.getUser(),fs.getPassword(),fs.getFilepath(),true));
    return root;
  }
 catch (  Exception e) {
  }
  return null;
}
