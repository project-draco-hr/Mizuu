{
  showProgressBar();
  shownShows.clear();
  for (int i=0; i < shows.size(); i++) {
    if (shows.get(i).isFavorite())     shownShows.add(shows.get(i));
  }
  sortShows();
  notifyDataSetChanged();
  hideProgressBar();
}
