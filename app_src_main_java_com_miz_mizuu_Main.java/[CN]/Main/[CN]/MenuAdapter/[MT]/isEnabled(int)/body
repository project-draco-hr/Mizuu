{
  int type=mMenuItems.get(position).getType();
  if (type == MenuItem.SEPARATOR || type == MenuItem.SEPARATOR_EXTRA_PADDING || type == MenuItem.HEADER || type == MenuItem.SUB_HEADER)   return false;
  return true;
}
