{
  if (mOkHttpClient == null) {
    mOkHttpClient=new OkHttpClient();
    try {
      File cacheDir=getContext().getCacheDir();
      Cache cache=new Cache(cacheDir,25 * 1024 * 1024);
      mOkHttpClient.setCache(cache);
    }
 catch (    IOException e) {
    }
  }
  return mOkHttpClient;
}
