{
  if (mThreadPoolExecutor == null)   mThreadPoolExecutor=new ThreadPoolExecutor(1,2,2,TimeUnit.SECONDS,new LinkedBlockingQueue<Runnable>(),new PicassoThreadFactory());
  return mThreadPoolExecutor;
}
