{
  if (sPicassoDetailsView == null)   sPicassoDetailsView=new Picasso.Builder(context).downloader(getDownloader(context)).requestTransformer(getFileRequestTransformer()).build();
  return sPicassoDetailsView;
}
