{
  if (s == null)   return s;
 else {
    String result="";
    if (!s.isEmpty()) {
      char[] charArray=s.toCharArray();
      int count=charArray.length;
      for (int i=0; i < count; i++)       if (Character.isDigit(charArray[i]))       result+=charArray[i];
    }
    return result;
  }
}
