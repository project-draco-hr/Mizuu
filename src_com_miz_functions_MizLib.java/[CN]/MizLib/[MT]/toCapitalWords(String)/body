{
  if (s == null || s.isEmpty())   return "";
  StringBuilder result=new StringBuilder();
  String[] split=s.split("\\s");
  int count=split.length;
  for (int i=0; i < count; i++)   result.append(toCapitalFirstChar(split[i]) + " ");
  return result.toString().trim();
}
