{
  if (MizuuApplication.isFullscreen(this))   setTheme(R.style.Theme_Example_FullScreen);
 else   setTheme(R.style.Theme_Example);
  super.onCreate(savedInstanceState);
  getActionBar().setDisplayHomeAsUpEnabled(true);
}
