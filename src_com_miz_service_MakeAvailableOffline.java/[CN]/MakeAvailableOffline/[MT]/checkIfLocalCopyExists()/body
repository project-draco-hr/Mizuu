{
  File f=null;
  if (file.startsWith("http"))   f=MizLib.getOfflineFile(mContext,file);
 else   f=MizLib.getOfflineFile(mContext,smb.getCanonicalPath());
  return f.exists() && mSize == f.length();
}
