{
  File f=null;
  if (mFileUrl.startsWith("http"))   f=MizLib.getOfflineFile(mContext,mFileUrl);
 else   f=MizLib.getOfflineFile(mContext,mSmb.getCanonicalPath());
  return f.exists() && mSize == f.length();
}
