{
  super.onActivityResult(requestCode,resultCode,data);
  if (resultCode == 1) {
    forceLoaderLoad();
  }
 else   if (resultCode == 2 && mActionBar.getSelectedNavigationIndex() == FAVORITES) {
    showMovieSection(FAVORITES);
  }
 else   if (resultCode == 3) {
    notifyDataSetChanged();
  }
}
