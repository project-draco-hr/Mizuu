{
  if (isSmb) {
    try {
      isDirectory=smb.isDirectory();
    }
 catch (    SmbException e) {
      isDirectory=false;
    }
  }
 else {
    isDirectory=file.isDirectory();
  }
  return isDirectory;
}
