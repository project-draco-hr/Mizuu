{
  if (defaultMsg != null && activity != null)   activity.runOnUiThread(new Runnable(){
    public void run(){
      Toast.makeText(getActivity(),defaultMsg,Toast.LENGTH_LONG).show();
    }
  }
);
  mLoading=false;
}
