{
  if (!mHasSetBackground)   Palette.generateAsync(arg0,new PaletteAsyncListener(){
    @Override public void onGenerated(    Palette arg0){
      MizuuApplication.addToPaletteCache(mKey,arg0);
      setBackgroundColor(arg0,true);
    }
  }
);
  return arg0;
}
