{
  String newQuery=actor.replace("'","''");
  Cursor cursor=database.query(DATABASE_TABLE,SELECT_ALL,KEY_CAST + " like '%" + newQuery+ "%'",null,null,null,KEY_TITLE + " ASC");
  return cursor.getCount();
}
