{
  if (arg1 == LoadedFrom.DISK || arg1 == LoadedFrom.NETWORK)   cover.setAlpha(0f);
  cover.setImageBitmap(arg0);
  if (arg1 == LoadedFrom.DISK || arg1 == LoadedFrom.NETWORK)   cover.animate().alpha(1f).setDuration(250);
}
