{
  if (mCardContent != null) {
    mCardContent.addView(child,index,params);
    if (mProgressBar.getVisibility() == VISIBLE)     setProgressBarVisibility(false);
  }
 else {
    super.addView(child,index,params);
  }
}
